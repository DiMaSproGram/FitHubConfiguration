spring:
  jpa:
    database: POSTGRESQL
    properties.hibernate.temp.use_jdbc_metadata_defaults: false
    hibernate:
      ddl-auto: update
    properties:
  datasource:
    platform: postgres
    url: jdbc:postgresql://192.168.1.170:5432/training_service_database
    username: postgres
    password: 1111
    driver-class-name: org.postgresql.Driver

server:
  servlet:
    context-path: /training
  port: 8084